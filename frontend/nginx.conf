server {
    listen 80;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    # La SPA resuelve sus propias rutas: cualquier ruta desconocida devuelve
    # el index en lugar de un 404 del servidor.
    location / {
        try_files $uri $uri/ /index.html;
    }
}
